def db_setup():
    
    # Journey DB
    # Sub-Journey DB
    # Step DB
    # Event DB

    # Check if databases exist, if they do not create them, if they do then return the objects.

    return journey_db, sub_journey_db, step_db, event_db